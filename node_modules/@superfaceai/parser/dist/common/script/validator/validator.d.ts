import * as ts from 'typescript';
import { CharIndexSpan } from '../../source';
export declare type ValidatorDiagnostic = {
    detail: string;
    hints: string[];
    relativeSpan: CharIndexSpan;
};
export declare function validateScript(sourceNode: ts.Node, sourceText: string): ValidatorDiagnostic[];
