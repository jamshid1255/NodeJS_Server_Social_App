import { SecurityType } from '@superfaceai/ast';
import { ISecurityHandler, SecurityConfiguration } from '../../security';
import { AuthenticateRequestAsync } from '../interfaces';
export declare class HttpHandler implements ISecurityHandler {
    readonly configuration: SecurityConfiguration & {
        type: SecurityType.HTTP;
    };
    constructor(configuration: SecurityConfiguration & {
        type: SecurityType.HTTP;
    });
    authenticate: AuthenticateRequestAsync;
}
